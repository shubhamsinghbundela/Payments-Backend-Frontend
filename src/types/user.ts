export type SignupInput = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type SigninInput = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};
